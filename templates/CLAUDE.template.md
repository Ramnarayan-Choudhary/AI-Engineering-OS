# CLAUDE.md

Project instructions for Claude Code. Claude reads this automatically on session start.

## First steps every session

1. Read `AGENTS.md` (project facts) and the AI Engineering OS at `OS_PATH/START-HERE.md` (reusable behavior).
2. Classify the task (trivial / small / non-trivial / research).
3. For non-trivial work: plan and wait for approval before large edits.
4. Validate before claiming done; report Changed / Validated / Not validated / Risks / Next step.

## Project rules

TODO: Add project-specific rules that override or extend the OS defaults.
(Safety rules from the OS always apply and are never overridden.)

## Commands Claude should know

```bash
# Test:
# Lint / typecheck:
# Run:
```

TODO: Fill these so Claude validates with the right commands.
